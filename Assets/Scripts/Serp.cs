using UnityEngine;

public class Serp : MonoBehaviour
{   // Les GameObject apparait dans l'éditeur
    public GameObject A;
    public GameObject B;
    public GameObject C;
    public GameObject miDistance;
    public GameObject origine;


    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {  // Je choisi entre A, B et C mon préféré
        origine = A;

        for (int i = 0; i < 10000; i++)
        {
            if (origine = A)
            {

                int rng = Random.Range(0, 2);
                //si mon random est 0 je choisi B sinon je choisi C
            }



        }

    }

    // Update is called once per frame
    void Update()
    {

    }
}
    

