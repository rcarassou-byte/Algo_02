using UnityEngine;

//public class Serp : MonoBehaviour
//{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    //public GameObject B;
    //public GameObject origine, destination;
    //void Start()
    //{
        //origine = B;
        //for(int i = 0; i < 10000; i++)
       // {
            //if(origine == B)
            //Random.Range(,); // choisir un object entre A, B, C
       // }
        //Vector3 milieu = Vector3.Lerp(B.transform.position, destination, 0.5f); // B est mon objet préféré
    //}


    // Update is called once per frame